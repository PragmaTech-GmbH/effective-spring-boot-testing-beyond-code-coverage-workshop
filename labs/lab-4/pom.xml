<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.0.2</version>
    <relativePath/> <!-- lookup parent from repository -->
  </parent>
  <groupId>pragmatech.digital.workshops</groupId>
  <artifactId>lab-4</artifactId>
  <version>1.0.0</version>
  <name>lab-4</name>
  <description>Spring Boot Testing Workshop - Lab 4</description>

  <properties>
    <java.version>21</java.version>
    <wiremock.version>3.13.0</wiremock.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webflux</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-mail</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-freemarker</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-docker-compose</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- Solve "java.lang.ClassNotFoundException: io.netty.resolver.dns.MacOSDnsServerAddressStreamProvider" on MacOS -->
    <dependency>
      <groupId>io.netty</groupId>
      <artifactId>netty-resolver-dns-native-macos</artifactId>
      <version>4.2.12.Final</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-flyway</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webflux-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-restclient-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-flyway-test</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- WireMock — used for OpenLibrary stubs AND as a fake OIDC issuer (no Keycloak in lab 3) -->
    <dependency>
      <groupId>org.wiremock</groupId>
      <artifactId>wiremock-standalone</artifactId>
      <version>${wiremock.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <argLine>-XX:+EnableDynamicAgentLoading</argLine>
        </configuration>
      </plugin>
      <!--
        ╔══════════════════════════════════════════════════════════════════╗
        ║  PIT Mutation Testing Plugin                                    ║
        ║                                                                 ║
        ║  What it does:                                                  ║
        ║  PIT (PITest) automatically injects small bugs ("mutations")    ║
        ║  into your compiled bytecode, then reruns your unit tests.      ║
        ║  If a test fails → the mutant is "killed" (good).               ║
        ║  If all tests pass → the mutant "survived" (test gap found).    ║
        ║                                                                 ║
        ║  Why we need it:                                                ║
        ║  JaCoCo line coverage only tells you which code was EXECUTED,   ║
        ║  not whether your tests actually VERIFY anything. PIT answers   ║
        ║  the real question: "If this code had a bug, would my tests     ║
        ║  catch it?"                                                     ║
        ║                                                                 ║
        ║  How to run:                                                    ║
        ║  ./mvnw test-compile org.pitest:pitest-maven:mutationCoverage   ║
        ║  Then open: target/pit-reports/index.html                       ║
        ╚══════════════════════════════════════════════════════════════════╝
      -->
      <plugin>
        <groupId>org.pitest</groupId>
        <artifactId>pitest-maven</artifactId>
        <version>1.19.1</version>
        <dependencies>
          <!--
            REQUIRED for JUnit 5 support. Without this dependency, PIT
            silently finds zero tests and reports 0 mutants killed.
            Since version 1.2.0, the launcher version is auto-selected
            to match your JUnit 5 version.
          -->
          <dependency>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-junit5-plugin</artifactId>
            <version>1.2.1</version>
          </dependency>
        </dependencies>
        <configuration>
          <!--
            targetClasses: Which production classes PIT should mutate.
            Focus on classes with real business logic — services, domain,
            utils. Avoid controllers, repositories, config, and DTOs.
          -->
          <targetClasses>
            <param>pragmatech.digital.workshops.lab4.service.*</param>
          </targetClasses>

          <!--
            targetTests: Which test classes PIT should run against mutants.
            Only fast unit tests belong here. Exclude integration tests
            (*IT.java) — PIT reruns tests many times and slow tests make
            it unusable (hours instead of seconds).
          -->
          <targetTests>
            <param>pragmatech.digital.workshops.lab4.*Test</param>
          </targetTests>
          <excludedTestClasses>
            <param>**.*IT</param>
            <param>**.*IntegrationTest</param>
          </excludedTestClasses>

          <!--
            threads: PIT processes mutants in parallel. More threads =
            faster runs. Set to the number of CPU cores available.
          -->
          <threads>4</threads>

          <!--
            timestampedReports: When false, PIT overwrites the previous
            report instead of creating a new timestamped folder each run.
            Keeps target/pit-reports/ clean during development.
          -->
          <timestampedReports>false</timestampedReports>

          <!--
            mutationThreshold: Fail the build if the mutation score drops
            below this percentage. Start low (60-70%) and ratchet up over
            time. 80% is a good long-term target for business logic.
          -->
          <mutationThreshold>80</mutationThreshold>

          <!--
            avoidCallsTo: PIT won't generate "remove void method call"
            mutants for methods in these packages. Logging calls are a
            common source of noise — removing a log.info() never breaks
            a test, so the surviving mutant is meaningless.
          -->
          <avoidCallsTo>
            <avoidCallsTo>org.slf4j</avoidCallsTo>
            <avoidCallsTo>org.apache.logging</avoidCallsTo>
          </avoidCallsTo>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
